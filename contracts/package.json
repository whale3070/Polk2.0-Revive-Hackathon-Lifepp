{
  "name": "lifeplusplus-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:revive": "npx hardhat run scripts/deploy.js --network revive",
    "deploy:revive-local": "npx hardhat run scripts/deploy.js --network revive-local",
    "claim:revive-local": "npx hardhat run scripts/claim-local-tokens.js",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
